# Project Description
- 이 프로젝트 폴더는 머신러닝 / 계산화학 관련 연구 및 실험을 진행합니다
- 해당 프로젝트는 Universal Machine Learning Interatomic Potential(uMLIP)가 예측에 실패하는 High Entropy Alloy 시스템에서의 에너지 예측 불확실성을 Physical inspired representation을 과의 representatation alignment 를 측정하는 방식으로 간접적으로 추정하는 방법에 대해서 연구합니다. 더 나아가, 이 가설이 옳다면, aligment를 예측 불확실성을 대리하여 active learning 의 데이터 선택 기존으로 삼았을 때, 학습 효율의 높아지는 정도 또한 연구하고자 합니다.

# 일반적 workflow
- 다음의 단계를 통해서 representatation alignment가 uMLIP의 예측의 불확실성을 효과적으로 추정할 수 있는지 검증합니다
- (1) HEA 시스템으로 이루어진 원자 데이터셋 준비
- (2) uMLIP으로 에너지 예측을 진행 및 오차를 측정 / 저장
- (3) Physic-inspired representation 으로 해당 HEA 시스템을 각각 변환 및 저장. 
- (4) uMLIP 의 latent-vector를 추출하여 HEA 시스템을 각각 변환 및 저장.
- (5) 동일 시스템의 Physic-inspired representation 과 uMLIP 의 latent-vector 사이의 유사도를 측정 및 저장
- (6) 측정된 유사도와 uMLIP의 예측 오차 사이의 관계성을 측정

# 연구를 통해 검증되어야 하는 사항
- (1) 에너지 예측 오차와 가장 관계성이 높은 physical inspired representation은 무엇인가?
- (2) 에너지 예측 오차와 (physical inspired representation - MLIP latent vector alignment) 사이의 유의미한 관계성이 존재하는가?
- (3) physical inspired representation - MLIP latent vector alignment 을 active learning에서 uncertainty proxy으로 사용하였을 때, 유의미한 학습 효율성 증가가 존재하는가?

# 가상환경 사용 가이드
- 각각의 MLIP 마다 실행해야 하는 가상환경이 다를 수 있음
- 가상환경 fairchem 에서 실행: UMA 기반 MLIP
- 가상환경 fairchem1 에서 실행: Equiformer 기반 MLIP

# 코드베이스 리팩토링 진행상황

## ✅ 완료된 단계

### Step 1: 인프라 구조 설정 (완료)
- **백업 생성**: `/DATA/user_scratch/pn50212/2024/12_AtomAttention_code_backup_20250224/`
- **디렉토리 구조**: 모듈화된 src/, scripts/, configs/, tests/, legacy/ 생성
- **레거시 파일 정리**: 26개 rcut 파일들과 테스트 파일들 legacy/로 이동

### Step 2: 핵심 모듈 마이그레이션 (완료)
- **CKNNA 통합**: `src/core/cknna.py` - 표준 CKNNA 구현, 배치 처리, Active Learning
- **Alignment 통합**: `src/core/alignment.py` - 26개 rcut 파일 → 단일 파라미터화 모듈
- **테스트 완료**: 모든 핵심 기능 테스트 통과

### Step 3: Representation 추출 모듈화 (완료)
- **Base 클래스**: `src/representations/base.py` - 통합 API 설계
- **Physics-inspired**: `src/representations/physics_inspired/soap.py` - SOAP 통합
- **MLIP Embeddings**:
  - `src/representations/mlip_embeddings/equiformer.py` - EquiformerV2 통합
  - `src/representations/mlip_embeddings/mace.py` - MACE 통합
  - `src/representations/mlip_embeddings/uma.py` - UMA 통합
- **Hybrid 지원**: 다중 representation 조합 가능

### Step 4: 레거시 코드 정리 (완료)
- **39개 파일 삭제**: 중복된 old 파일들 제거 완료
- **실험 결과 정리**: alignment analysis 결과들 체계적 정리
- **백업 보관**: 모든 삭제 파일 안전하게 백업

## 📊 달성된 성과
- **85% 코드 감소**: 70+ 파일 → 깔끔한 모듈 구조
- **중복 제거**: 26개 rcut 파일 → 1개 파라미터화 모듈
- **API 통합**: 모든 representation 추출기가 동일한 인터페이스 사용
- **테스트 완료**: 모든 핵심 기능 검증 완료

## 🚀 새로운 사용법

### CKNNA 계산 (기존 10개 파일 → 1개 모듈)
```python
from src.core.cknna import CKNNAAnalyzer
analyzer = CKNNAAnalyzer()
results, correlations = analyzer.analyze_representations(soap_data, mlip_data, errors)
```

### Alignment 분석 (기존 26개 파일 → 1개 모듈)
```python
from src.core.alignment import SOAPAlignmentAnalyzer
analyzer = SOAPAlignmentAnalyzer()
rcut_results = analyzer.analyze_rcut_range(soap_loader, mlip_repr, errors, [4,6,8,10,12,14])
```

### Representation 추출 (기존 12개 파일 → 4개 모듈)
```python
# SOAP 추출
from src.representations import SOAPExtractor, SOAPConfig
extractor = SOAPExtractor(SOAPConfig(r_cut=6.0))
soap_repr = extractor.extract_single(atoms, atom_selection="multi")

# EquiformerV2 추출
from src.representations import create_equiformer_extractor
extractor = create_equiformer_extractor('eq2_31M_ec4_allmd')
eq_repr = extractor.extract_single(atoms)

# 하이브리드 representation
from src.representations import HybridRepresentation
hybrid = HybridRepresentation({'soap': soap_ext, 'equiformer': eq_ext})
all_repr = hybrid.extract_all(atoms)
```

## 📁 현재 디렉토리 구조
```
12_AtomAttention/
├── src/                                    # 새로운 모듈화된 소스 코드
│   ├── core/                              # 핵심 기능 (CKNNA, Alignment)
│   ├── representations/                   # 통합된 representation 추출
│   └── [data_efficiency/, models/, utils/] # 다음 단계 예정
├── scripts/                               # 실행 스크립트 및 마이그레이션 데모
├── tests/                                 # 포괄적 테스트 스위트
├── data/results/                          # 정리된 실험 결과
├── legacy/                               # 백업된 기존 파일들
└── deleted_files_backup_20260224/        # 삭제된 파일 백업
```

## 🎯 다음 단계 (필요시)
1. **Data Efficiency 실험** 모듈화 (`data_efficiency_experiment_*.py`)
2. **설정 시스템** 구축 (YAML 기반)
3. **CLI 통합** (명령행 인터페이스)

## 📖 마이그레이션 참고
- **MIGRATION_PROGRESS.md**: 상세 진행 상황
- **scripts/migrate_*.py**: 사용법 데모 스크립트
- **tests/test_*.py**: 기능 검증 테스트
